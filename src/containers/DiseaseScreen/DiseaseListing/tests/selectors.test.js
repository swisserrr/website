// import { selectDiseaseListingDomain } from '../selectors';

describe('selectDiseaseListingDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
