// import { selectRequestLocationDomain } from '../selectors';

describe('selectRequestLocationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
