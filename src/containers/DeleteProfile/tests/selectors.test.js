// import { selectDeleteProfileDomain } from '../selectors';

describe('selectDeleteProfileDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
