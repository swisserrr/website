// import { selectCareHealthInsuranceDomain } from '../selectors';

describe('selectCareHealthInsuranceDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
