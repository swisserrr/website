// import { selectUserStoriesListingDomain } from '../selectors';

describe('selectUserStoriesListingDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
