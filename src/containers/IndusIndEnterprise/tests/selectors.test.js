// import { selectIndusIndEnterpriseDomain } from '../selectors';

describe('selectIndusIndEnterpriseDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
