// import { selectBusinessPartnerDomain } from '../selectors';

describe('selectBusinessPartnerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
