// import { selectAccertifySurveyDomain } from '../selectors';

describe('selectAccertifySurveyDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
