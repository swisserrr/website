// import { selectMohtvSubCategoryPageDomain } from '../selectors';

describe('selectMohtvSubCategoryPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
