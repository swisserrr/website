// import { selectEmergencyDetailDomain } from '../selectors';

describe('selectEmergencyDetailDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
