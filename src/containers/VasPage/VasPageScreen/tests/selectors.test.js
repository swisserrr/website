// import { selectDiseaseScreenDomain } from '../selectors';

describe('selectDiseaseScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
